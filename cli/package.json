{
  "name": "supersurf-cli",
  "version": "3.4.0",
  "private": true,
  "description": "The `supersurf` command-line entrypoint — compiled to a standalone binary by Bun, never published to npm",
  "license": "Apache-2.0",
  "scripts": {
    "build": "bun run build.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test.watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "acorn": "^8.14.0",
    "acorn-walk": "^8.3.4",
    "commander": "^14.0.1",
    "shared": "*",
    "typescript": "^5.5.0",
    "vitest": "^4.0.18"
  }
}
